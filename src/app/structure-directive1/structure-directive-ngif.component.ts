import { Component } from '@angular/core';

@Component({
  selector: 'app-structure-directive1',
  templateUrl: './structure-directive-ngif.component.html',
  styleUrls: ['./structure-directive-ngif.component.css']
})
export class StructureDirectiveNgifComponent {

  // showcontent(){
  //   // this.cols=Array.from({length:numcols},(_,index) =>index+1);
  //   // this.rows=Array.from({length:numrows},(_,index) =>
  //   // this,cols.map(cols => (rowIndex + 1)* col);
  //
  // }




}
